#include"holberton.h"
/**
 * factorial - returns the factorial of a given number
 * @n: integer
 * Return: integer of number
 */
int factorial(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	else if (n == 0)
	{
		return (1);
	}

	return (0);
}
