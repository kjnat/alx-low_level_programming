#include <stdio.h>
/*
 * main - entry function
 * Description: printing lowercase alphabets
 * Result: 0 is success
 */
int main()
{
        int i;

	for(i = 'a'; i <= 'z'; i++)
        {
	        i = toLower(i);
                putchar(i);
	}
        return (0);
}
