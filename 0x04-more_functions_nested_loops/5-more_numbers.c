
#include "holberton.h"

/**
 * more_numbers - prints 10x numbers from 0 to 14.
 * Return: no return.
 */
void more_numbers(void)
{
	int a;

	for (a = 0; a <= 14; ch++)
	{ 
    int a = 0;
    while (a < 9)
    {
      _putchar(a);
      a++;
      }
		_putchar(ch);
	}
	_putchar('\n');
}
